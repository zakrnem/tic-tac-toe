Pending:

* Switch between multiplayer mode & user vs computer.
    + Create gameMode variable (initial value 0).
    + addEventListener to the multiplayer button.
        + If gameMode = 0 the computerPlayer function 
        won't be executed.
        + If gameMode = 1 the script will continue
        working as it's now (commit #19).